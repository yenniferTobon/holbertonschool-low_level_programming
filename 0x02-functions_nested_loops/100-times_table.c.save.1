#include "holberton.h"
#include <stdio.h>

void print_times_table(int n)
{
	if (n >= 0 && n <= 15)
	{
		int fin, i, j;

		for (i = 0; i <= ; c++)
		{
			for (i = 0; i <= n; i++)
			{
				result = i * c;
				if (result > 99)
				{
					_putchar(result / 100 + '0');
					_putchar(result % 100 / 10 + '0');
					_putchar(result % 100 % 10 + '0');
				}
				else if (result > 9)
				{
					_putchar(' ');
					_putchar(result / 10 + '0');
					_putchar(result % 10 + '0');
				}
				else
				{
					if (i != 0)
					{
						_putchar(' ');
						_putchar(' ');
					}
					_putchar(result + '0');
				}
				if (i != n)
				{
					_putchar(',');
					_putchar(' ');
				}
			}
			_putchar('\n');
		}
	}
}
